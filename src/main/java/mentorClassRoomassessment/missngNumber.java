package mentorClassRoomassessment;

import java.util.Arrays;

public class missngNumber {
	
	/****
	 * https://leetcode.com/problems/missing-number/description/
	 * Sample test data : Input: nums = [0,1]
	 * Input as int arr
	 * Output as int
	 * Sort the given array
	 * Iterate the loop until array values
	 * Condition to check if(array[i]!=i)then return num value as index[i]
	 * Check for the condition num=== length of array and return the length
	 * @return 
	 * 
	 */
	
	public int missing_num_arry(int[] arr)
	{
		int num =0;
		int len = arr.length;
		Arrays.sort(arr);
		return 0;
		
		
	}

}
